import XCTest
@testable import ZSlider

final class ZSliderTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
       // XCTAssertEqual(ZSlider().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
